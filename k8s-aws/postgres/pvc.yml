apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: smartschool
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2-csi  
  resources:
    requests:
      storage: 10Gi 