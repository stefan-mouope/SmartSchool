apiVersion: v1
kind: Service
metadata:
  name: authentification-service
  namespace: smartschool
spec:
  selector:
    app: authentification-service
  type: NodePort
  ports:
    - port: 8001
      targetPort: 8001
