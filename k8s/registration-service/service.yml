apiVersion: v1
kind: Service
metadata:
  name: registration-service
  namespace: smartschool
spec:
  selector:
    app: registration-service
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
